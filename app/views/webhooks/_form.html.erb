<%= form_with(model: webhook, local: true) do |form| %>
  <% if webhook.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(webhook.errors.count, "error") %> prohibited this webhook from being saved:</h2>

      <ul>
      <% webhook.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :login %>
    <%= form.text_field :login, id: :webhook_login %>
  </div>

  <div class="field">
    <%= form.label :repo %>
    <%= form.text_field :repo, id: :webhook_repo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
